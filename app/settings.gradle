pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            name = 'sdl-release'
            url = 'https://nexus.devops.sec.samsung.net/repository/sdl-releases/'
        }
    }
}

rootProject.name = 'rabbitmq-cluster-sample'
